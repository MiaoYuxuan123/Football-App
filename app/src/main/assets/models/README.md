Put your player model file here:

- Required filename: `player.glb`
- Path: `app/src/main/assets/models/player.glb`

The Live 3D WebView loads this file first. If not found, it falls back to a public demo model so interaction still works.

